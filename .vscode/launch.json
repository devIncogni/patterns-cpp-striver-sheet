{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB Debug CMake",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/main",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake Build Debug"
        }
    ]
}

